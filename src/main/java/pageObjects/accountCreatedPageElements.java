package pageObjects;

public interface accountCreatedPageElements {
    
    // Header
    String hdrAccountCreated = "//b[normalize-space()='Account Created!']";

    // Home Tab / Continue Button
    String btnContinue = "//a[normalize-space()='Continue']";
}
