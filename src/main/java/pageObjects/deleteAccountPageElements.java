package pageObjects;

public interface deleteAccountPageElements {
    
    // Header
    String hdrAccountDeleted = "//b[normalize-space()='Account Deleted!']";

    // Home Tab / Continue Button
    String btnContinue = "//a[normalize-space()='Continue']";
}
